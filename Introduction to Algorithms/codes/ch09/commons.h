#ifndef COMMONS_H_
#define COMMONS_H_

/* Display an integer array */
void showarray(int a[], int n);

/* Generate random integer between 0 to 99 */
void random_integer(int a[], int n);

/* swap two elements */
void swap_int(int *a, int *b);

/* generate an random int between p to q */
int generate_random_int(int p, int q);
#endif
