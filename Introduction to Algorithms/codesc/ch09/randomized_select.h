#ifndef RANDOMIZED_SELECT_H_
#define RANDOMIZED_SELECT_H_

/* a is an array, p is start point, r is end point, i 
   is the order; return the ith smallest element */
int randomized_select(int a[], int p, int r, int i);

#endif